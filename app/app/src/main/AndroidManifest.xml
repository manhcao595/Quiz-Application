<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basic_MC">

        <activity
            android:name=".activity.AdminHomeActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ExamManagementActivity"
            android:exported="false" />

        <activity
            android:name=".activity.QuestionManagementActivity"
            android:exported="false" />

        <activity
            android:name=".activity.StatisticsActivity"
            android:exported="false" />

        <activity
            android:name=".activity.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".activity.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ExamListActivity"
            android:exported="false"/>

        <activity
            android:name=".activity.QuizActivity"
            android:exported="false"/>

        <activity android:name=".activity.ResultActivity" />
        <activity android:name=".activity.ResultDetailActivity" />
        <activity android:name=".activity.HistoryActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>